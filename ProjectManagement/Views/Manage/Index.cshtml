@model IndexViewModel
@{
    ViewData["Title"] = "Manage your account";
}

<h2 style="margin-top: 100px;">@ViewData["Title"].</h2>
<p class="text-success">@ViewData["StatusMessage"]</p>

<div>
    <h4>Change your account settings</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Username: </dt>
        <dd>
            @Model.UserName
        </dd>
        <dt>Email: </dt>
        <dd>
            @Model.Email
        </dd>
        <dt>First name: </dt>
        <dd>
            @Model.FirstName
        </dd>
        <dt>Last name: </dt>
        <dd>
            @Model.LastName
        </dd>
        <dt>Password:</dt>
        <dd>
            @if (Model.HasPassword)
            {
                <a asp-controller="Manage" asp-action="ChangePassword" class="btn-bracketed">Change</a>
            }
            else
            {
                <a asp-controller="Manage" asp-action="SetPassword" class="btn-bracketed">Create</a>
            }
        </dd>
        <dt>External Logins:</dt>
        <dd>
            @Model.Logins.Count <a asp-controller="Manage" asp-action="ManageLogins" class="btn-bracketed">Manage</a>
        </dd>
        <dd>
            <a asp-controller="Manage" asp-action="EditUserInfo" class="btn-bracketed">Edit user info</a>
        </dd>
    </dl>
</div>
